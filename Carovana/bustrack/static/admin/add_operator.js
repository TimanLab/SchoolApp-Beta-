function submitAddOperatorForm() {
    var name = document.getElementById("name").value;
    var bus = document.getElementById("bus").value;

    // Add your logic to handle the form submission (e.g., AJAX request to the server)

    // For demonstration purposes, let's show an alert for successful submission
    alert("Operator added successfully!");
    // You can implement further logic like clearing the form or closing the modal
}
